<!DOCTYPE html>
@using StudyBuddy.Web.Models.ViewModels
@model PartnerSearchViewModel

@{
    ViewData["Title"] = "Find Study Partner";
}

<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <div>
            <h2>Automatic Partner Matching</h2>
            <p class="text-muted mb-0">
                Here are recommended study partners based on your interests.
            </p>
        </div>
        <a asp-action="EditProfile"
           asp-controller="PartnerMatching"
           class="btn btn-outline-primary">
            Edit My Profile
        </a>
    </div>

    @if (Model?.Results != null && Model.Results.Count > 0)
    {
        <div class="row mt-4">
            @foreach (var partner in Model.Results)
            {
                <div class="col-md-6 col-lg-4 mb-3">
                    <div class="card shadow-sm h-100">
                        <div class="card-body d-flex flex-column">
                            <h5 class="card-title">@partner.Subject</h5>
                            <p class="card-text mb-2">
                                <strong>Faculty:</strong> @partner.Faculty<br />
                                <strong>Level:</strong> <span class="badge bg-info">@partner.Level</span>
                            </p>

                            <div class="mt-auto">
                                <form asp-action="MarkFavorite"
                                      asp-controller="PartnerMatching"
                                      asp-route-partnerId="@partner.Id"
                                      method="post"
                                      class="d-inline">
                                    @Html.AntiForgeryToken()
                                    <button type="submit"
                                            class="btn btn-sm @(partner.IsFavorite ? "btn-warning" : "btn-outline-warning")">
                                        @(partner.IsFavorite ? "Favorite" : "Mark as Favorite")
                                    </button>
                                </form>

                                <a href="#"
                                   class="btn btn-sm btn-primary ms-1">
                                    Send Request
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    }
    else
    {
        <div class="alert alert-info mt-4">
            No matches found for your current profile.
            <a asp-action="EditProfile" asp-controller="PartnerMatching">Edit profile</a>
            or
            <a asp-action="Search" asp-controller="PartnerMatching">try manual search</a>.
        </div>
    }

    <div class="mt-4">
        <a asp-action="Search"
           asp-controller="PartnerMatching"
           class="btn btn-secondary">
            Manual Partner Search
        </a>
    </div>
</div>
