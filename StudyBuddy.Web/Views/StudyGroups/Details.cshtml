@{
    ViewData["Title"] = Model.Name;
}

<div class="container mt-5">
    <h2>@Model.Name</h2>
    <p class="text-muted">Grupa kreirana: @Model.CreatedAt.ToString("dd.MM.yyyy HH:mm")</p>

    <div class="row mt-4">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">📋 Detalji grupe</h5>
                </div>
                <div class="card-body">
                    <p><strong>Status:</strong> <span class="badge bg-success">@(Model.IsActive ? "Aktivna" : "Neaktivna")</span></p>
                    <p><strong>Vlasnik ID:</strong> @Model.OwnerId</p>

                    <div class="mt-3">
                        <a asp-action="Members" asp-route-id="@Model.Id" class="btn btn-info">Članovi</a>
                        <a asp-controller="Schedule" asp-action="Index" asp-route-groupId="@Model.Id" class="btn btn-warning">Raspored</a>
                        <a asp-action="Index" class="btn btn-secondary">Nazad</a>
                    </div>
                </div>
            </div>
        </div>

        <div class="col-md-4">
            <div class="card">
                <div class="card-header bg-info text-white">
                    <h5 class="mb-0">⚙️ Akcije</h5>
                </div>
                <div class="card-body">
                    <a asp-action="Members" asp-route-id="@Model.Id" class="d-block mb-2 btn btn-sm btn-outline-primary">
                        Upravljaj članovima
                    </a>
                    <a asp-controller="Schedule" asp-action="Create" asp-route-groupId="@Model.Id" class="d-block mb-2 btn btn-sm btn-outline-success">
                        Zakaži novu sesiju
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>